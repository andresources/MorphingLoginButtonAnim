<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yallamredy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MorphingLoginButtonAnim">
        <activity
            android:name=".sample2.A2_NewActivity"
            android:exported="false" />
        <activity
            android:name=".sample1.SecondActivity"
            android:exported="false" /> <!-- .sample1.MainActivity -->
        <activity
            android:name=".sample2.A2_MainActivity"
            android:theme="@style/login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>